# Micro-frontends com Module Federation

## Estrutura
- **container/** → app host (carrega os remotes)
- **menu/** → microfrontend de menu
- **order/** → microfrontend de pedidos

## Rodar localmente
Em cada pasta:
```bash
npm install
npm start
